name: Bloomberg Surveillance Daily Summary

on:
  schedule:
    # Runs at 13:00 UTC (14:00 CET / 08:00 ET) Mon-Fri
    # Plus 60 min delay = fetch at 14:00 UTC (15:00 CET)
    - cron: '0 14 * * 1-5'
  workflow_dispatch:

jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Process Bloomberg Surveillance
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
        run: python3 process_show.py bloomberg_surveillance

      - name: Commit state and summaries
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add .last_processed_* summaries/ transcripts/ || true
          git diff --quiet && git diff --staged --quiet || git commit -m "Update Bloomberg Surveillance [automated]"
          git push || true
